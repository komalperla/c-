#include <iostream>
#include<string>
using namespace std;

int stringLength(char* str){
    int i;
    for(i=0;str[i]!='\0';i++);
    return i;
}

int main(){   //DO NOT change the 'main' signature
    char str[100];
    cin>>str;
    int length=stringLength(str);
    cout<<length;
    return 0;
}
